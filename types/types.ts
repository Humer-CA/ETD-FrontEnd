// Define your Post interface
export interface Post {
  username: string;
  password: string;
}

// Define response types if needed
export interface PostResponse {
  // Define the response structure here
  success: boolean;
  message: string;
}
